## Features

### Dark theme toggle
A button in the header switches between light and dark mode.
Clicking it toggles a `data-theme="dark"` attribute on `<body>`,
and CSS uses that attribute to swap the colour palette.
